apiVersion: v1
kind: Service
metadata:
  name: {{ include "personal-car-assistant.fullname" . }}
  labels:
    {{- include "personal-car-assistant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "personal-car-assistant.selectorLabels" . | nindent 4 }}
