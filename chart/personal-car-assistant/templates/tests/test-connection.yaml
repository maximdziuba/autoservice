apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "personal-car-assistant.fullname" . }}-test-connection"
  labels:
    {{- include "personal-car-assistant.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "personal-car-assistant.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
